# Використовуємо офіційний образ Node.js як базовий
FROM node:14

# Встановлюємо робочу директорію в контейнері
WORKDIR /usr/src/app

# Копіюємо package.json і package-lock.json до робочої директорії
COPY package*.json ./

# Встановлюємо залежності
RUN npm install

# Копіюємо решту файлів додатку до робочої директорії
COPY . .

# Експонуємо порт 3000 для доступу до сервера
EXPOSE 3000

# Вказуємо команду для запуску серверного додатку
CMD ["node", "server.js"]
